<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hel_ver2">

    <!-- 권한 선언 -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.VIBRATE" /> <!-- 진동 권한 추가 -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />


    <!-- 전화 기능이 필요하지 않음을 명시 -->
    <uses-feature android:name="android.hardware.telephony" android:required="false" />

    <!-- GPS 권한 선언 -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_hel"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_hel_foreground"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- MainActivity 선언 -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- SettingsActivity 선언 -->
        <activity
            android:name=".SettingsActivity"
            android:exported="true" />

        <!-- AnotherSettingActivity 선언 -->
        <activity
            android:name=".AnotherSettingActivity"
            android:exported="true" />
        <activity
            android:name=".LocationActivity" />
        <activity
            android:name=".TextActivity" />
        <!-- <activity
            android:name=".StatusActivity" /> -->
        <activity
            android:name=".AlertActivity">
        </activity>
    </application>

</manifest>
